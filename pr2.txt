Prolog fimaly tree :
% Facts

male(ramji).
female(bhimabai).
male(ambedkar).
female(ramabai).
female(savita).
male(yashwant).

% Parent relations
parent(ramji, ambedkar).
parent(bhimabai, ambedkar).

parent(ambedkar, yashwant).
parent(ramabai, yashwant).

% Spouse relations
spouse(ambedkar, ramabai).
spouse(ambedkar, savita).

% Rules

father(X, Y) :- male(X), parent(X, Y).
mother(X, Y) :- female(X), parent(X, Y).

grandparent(X, Y) :- parent(X, Z), parent(Z, Y).

sibling(X, Y) :- parent(P, X), parent(P, Y), X \= Y.

husband(X, Y) :- male(X), spouse(X, Y).
wife(X, Y) :- female(X), spouse(X, Y).
